const baseStyles = 'bg-transparent p-2 rounded w-full';

export const variantStyles = {
	box:
		'border-2 border-black/50 read-only:bg-gray-200 read-only:border-black/30 read-only:text-black/90' +
		baseStyles,
	underlined: 'border-b-2 border-black/50' + baseStyles,
};
